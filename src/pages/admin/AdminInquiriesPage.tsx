import Container from "../../components/common/Container";

export default function AdminInquiriesPage() {
  return (
    <section className="page">
      <Container>
        <h1 className="hero-title">문의 관리</h1>
        <div className="empty-state">문의 목록이 여기에 표시됩니다.</div>
      </Container>
    </section>
  );
}
